name: 'Test'

on:
  issue_comment:
    types: [created]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: ./
        with:
          trigger: '/trigger'
